// This component is no longer used - the animation now happens directly on the PropertyDetails page
// Keeping file for backwards compatibility, but it renders nothing

export function PageTransitionOverlay() {
  return null;
}
